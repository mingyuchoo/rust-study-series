# Qdrant 연결 설정
# 실제 사용 시 이 파일을 ".env"로 복사하여 값을 채우세요.
# 예: PowerShell에서 `Copy-Item .env.example .env`

# 필수: Qdrant 서비스 URL
# 예) 도커 포트 포워딩: http://localhost:6334
QDRANT_URL=http://localhost:6334

# 필수: 컬렉션 이름 1 (기본 파라미터로 생성/삭제 테스트용)
QDRANT_COLLECTION_NAME_1=my_collection

# 필수: 컬렉션 이름 2 (커스텀 파라미터로 생성/삭제 테스트용)
QDRANT_COLLECTION_NAME_2=custom_collection

# 선택: 벡터 크기 (기본값 384)
QDRANT_VECTOR_SIZE=384
