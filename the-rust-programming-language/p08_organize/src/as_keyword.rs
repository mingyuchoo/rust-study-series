use std::fmt::Result; // 모듈 경로를 현재 범위 안으로 가져오기
use std::io::Result as IoResult; // 모듈 경로를 현재 범위 안으로 가져오기

// pub fn call1(result: Result) -> Result {
//   // need to implement here
// }
// pub fn call2(result: IoResult<()>) -> IoResult<(
//   // need to implement here
// )> {}
