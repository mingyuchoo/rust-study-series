# gpui

## Create a gpui project

```bash
cargo binstall create-gpui-app
create-gpui-app --workspace --name my-gpui-app
cd my-gpui-app
```

## Build and run

```bash
cargo build
# or
cargo build --release

cargo run
```


## References

- https://crates.io/crates/create-gpui-app
